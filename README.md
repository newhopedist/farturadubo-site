# FARTURADUBO Site

Site institucional desenvolvido em Next.js (App Router) com Tailwind CSS.

## Desenvolvimento
- Node 18+
- `npm install`
- `npm run dev`

## Estrutura
- `src/app` páginas e layout
- `src/components` componentes da UI
- `public/` assets estáticos

## Deploy
- Vercel recomendado

## Ambiente
Crie um arquivo `.env.local` com variáveis necessárias (ex.: chaves de APIs).